import * as React from 'react';

export interface IListsPageProps {
}

export function ListsPage (props: IListsPageProps) {
  return (
    <div className='container'>
      This is a simple page
    </div>
  );
}
